// File giữ chỗ cho cấu trúc states/ giống prototype.
// Hiện project đang dùng localStorage/sessionStorage trong services/authService.js.
export const AUTH_STORAGE_KEYS = {
  token: 'token',
  user: 'user'
};
